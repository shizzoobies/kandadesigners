# Claude Code internal files
.claude/

# WordPress export (not needed for the live site)
*.xml
!sitemap.xml
images from Wordpress/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Tooling / build
node_modules/
tailwind-build/node_modules/
.playwright-mcp/
.omc/
_shot_*.png
_home_preview.html
dist/
.astro/
.dev.vars
.wrangler/

# The admin app (admin/, its own Worker with its own lockfile) needs no extra
# entries: node_modules/, dist/, .astro/, .dev.vars and .wrangler/ above are
# unanchored, so they match at any depth.

# Generated D1 import SQL: derived from the committed JSON export, so it is
# reproducible with `node db/songs/import-from-firestore.mjs` and does not need
# to be tracked twice.
db/songs/generated/

# Working source drops (originals kept locally, web-ready copies live in images/)
public/images/New Images/
fonts-drop/
