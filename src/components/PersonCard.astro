---
const {
  name,
  slug,
  role,
  summary,
  skills = [],
  image = "/images/people/alex.jpg",
  accent = "studio",
} = Astro.props;

const href = `/team/${slug}`;
---

<a class={`personCard cardLink reveal accent-${accent}`} href={href} aria-label={`View ${name} profile`}>
  <div class="portrait" aria-hidden="true">
    <img src={image} alt={name} loading="lazy" decoding="async" />
    <div class="portraitOverlay"></div>
    <div class="portraitMeta">
      <span class="portraitTag">View</span>
    </div>
  </div>

  <div class="personBody">
    <div class="personTop">
      <div class="personName">{name}</div>
      <div class="personRole">{role}</div>
    </div>

    <p class="personSummary">{summary}</p>

    {skills?.length > 0 && (
      <div class="tagRow">
        {skills.slice(0, 6).map((t) => (
          <span class="tag">{t}</span>
        ))}
      </div>
    )}
  </div>
</a>
