---
import ArtistLayout from '../../layouts/ArtistLayout.astro';
import { artists } from '../../data/artists.js';

const artist = artists.find((a) => a.slug === 'artist-one');
---
<ArtistLayout artist={artist} />
